import { Dimensions, StyleSheet } from "react-native";


const styles = StyleSheet.create({
    container: {
        marginBottom:25,
        fontSize: 50,
        fontWeight: "bold",
        color: "white",
        textAlign:"center",
    }
});

export default styles;